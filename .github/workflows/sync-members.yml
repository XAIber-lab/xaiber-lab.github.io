name: Sync Members

# Runs on a schedule, and can be triggered manually from the Actions tab
# any time right after you edit the Google Sheet.
on:
  schedule:
    - cron: "0 7 * * *"   # daily at 07:00 UTC — adjust as you like
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          # Reuses the same personal access token set up for the
          # Publications sync — see README for why the default
          # token isn't enough in this org.
          token: ${{ secrets.PUBLICATIONS_PUSH_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Fetch sheets and regenerate member pages + members.html
        env:
          MEMBERS_SHEET_CSV_URL: ${{ vars.MEMBERS_SHEET_CSV_URL }}
          MEMBER_ACTIVITIES_SHEET_CSV_URL: ${{ vars.MEMBER_ACTIVITIES_SHEET_CSV_URL }}
        run: python scripts/sync_members.py

      - name: Commit changes if any
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add members.html members/
          if git diff --staged --quiet; then
            echo "No changes to member pages."
          else
            git commit -m "chore: sync members from Google Sheet [skip ci]"
            git push
          fi
